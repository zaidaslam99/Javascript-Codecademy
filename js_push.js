const itemTracker = ["0", "1", "2", "3", "4"];

itemTracker.push("5", "6");

console.log(itemTracker);


/*
    * So here we are going to be talking about the push method and 
    its similar to use the extend method when it comes to working with 
    python notice what we did was that we called the method and then we had 
    inputted the arguments and then printed the list now this is going to 
    give us a list holding the previous elements and then the two new elements 
    with it that is all

    * This is known as destructive array method meaning that its going to be 
    changing the array
*/

const chores = ["Laudry", "Dishes", "Cooking"];

chores.push("Grocery", "Sleeping")

console.log(chores);