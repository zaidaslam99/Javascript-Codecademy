/*
    * We are going to be using an array literal which creates an array by 
    wrapping items in square brackets arrays can store any type of data 
    we can have an array that hold same type of data or different either or.
*/

const hobbies = ["Basketball", "Coding", "Volleyball"];

console.log(hobbies);

/*
    * So here the thing that I wanted to mention is that we are 
    working with a simple list which is going to be having 3 different 
    elements in it array = list and the other thing is that when list 
    get printed in JS they have a space from opping ends on each elements
*/