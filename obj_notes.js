const random_obj = {
    name: "zaid",
    "age": 25,
    hobbies: "Basketball"
}

//console.log(random_obj.age);
//console.log(random_obj["age"]);

//console.log(random_obj.name);

// Will give a reference error
// console.log(random_obj[name]);

/*
    * Something that I wanted to learn here what that when we 
    when it comes to use object notation and bracket notation the 
    thing that we need to know here is simple that when we call 
    using the bracket we have to pass in the arg as "" other 
    wise its not going to work.
*/
